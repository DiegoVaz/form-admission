import { TextField, FormControl } from '@material-ui/core';

import './styles.module.css';

const PersonalInformationComponent = () => {
  return (
    <FormControl>
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
      <TextField defaultValue="Diego" size="small" disabled />
    </FormControl>
  );
};

export default PersonalInformationComponent;
